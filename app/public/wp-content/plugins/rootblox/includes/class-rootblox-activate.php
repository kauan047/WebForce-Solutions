<?php
namespace Rootblox;

class Rootblox_Activate {
	/**
	 * Handles plugin activation.
	 *
	 * This method is triggered when the plugin is activated.
	 * It can be used to set up database tables, register custom post types,
	 * flush rewrite rules, or initialize default settings.
	 *
	 * @return void
	 */
	public static function activate() {
	}
}
